# GoalFlow：用 gstack 梳理新功能范围

这份说明是给当前项目直接开用的，默认基于你这台机器上的 `gstack` 前缀命令。

项目现状：
- 产品定位：目标驱动型成长管理工具，不是日历/打卡/传统待办
- 核心模型：`目标 -> 阶段 -> 行动 -> 结果`
- 当前文档基础：`PRD.md`、`DEVELOPMENT_SPEC.md`
- 当前最适合 gstack 的起手场景：先把“新功能范围”梳理清楚，再进入实现

## 最推荐的起手顺序

先跑这 2 步：

1. `/gstack-office-hours`
2. `/gstack-plan-eng-review`

如果你这次要梳理的是“产品价值和边界”，第二步可以改成：

1. `/gstack-office-hours`
2. `/gstack-plan-ceo-review`

简单记忆：
- 偏“这个功能值不值得做、边界怎么收” -> `/gstack-plan-ceo-review`
- 偏“这个功能怎么落、接口和边界条件怎么定” -> `/gstack-plan-eng-review`

## 直接照抄的话术

### 方案一：先梳理新功能范围

先输入：

```text
/gstack-office-hours

我在做一个微信小程序项目 GoalFlow（成长薄）。
现有产品不是日历工具，也不是打卡工具，而是“目标驱动型成长管理工具”。
核心逻辑是：目标 -> 阶段 -> 行动 -> 结果。

当前我已经有 PRD.md 和 DEVELOPMENT_SPEC.md。
请你基于现有产品定位，帮我梳理一个“新功能”的范围，重点回答：
1. 这个功能解决的核心问题是什么
2. 它属于核心闭环，还是辅助能力
3. 它和现有目标/阶段/任务/明日代办/复盘的关系是什么
4. V1 最小范围应该包含什么，不该包含什么
5. 哪些需求看起来合理，但其实应该延后

先不要写代码，先把范围、边界、最小可行版本讲清楚。
```

### 方案二：在产品范围基础上收敛工程实现

在 `/gstack-office-hours` 跑完后，再输入：

```text
/gstack-plan-eng-review

基于刚才梳理出的新功能范围，请继续从工程实现角度收敛方案。
项目当前参考文档是 PRD.md 和 DEVELOPMENT_SPEC.md。

请重点输出：
1. 这个功能应该落在哪个模块
2. 需要新增还是复用哪些数据结构
3. 是否影响首页聚合、计划、阶段、任务、明日代办、复盘这些现有模块
4. API、状态流转、排序规则、边界条件上要注意什么
5. V1 实现时最容易做过头的地方是什么

先输出清晰的实现边界和拆分建议，不要直接进入编码。
```

### 方案三：如果你更关心产品边界而不是实现

把第二步换成：

```text
/gstack-plan-ceo-review

基于刚才的功能范围，请从产品价值、用户感知和版本优先级角度继续收敛。

请重点判断：
1. 这个功能是不是 GoalFlow 当前 V1 应该做的
2. 它会不会破坏“目标驱动”而变成日历/打卡/传统待办思路
3. 用户真正想要的是这个功能本身，还是它背后的某个结果
4. 最小上线版本应该砍掉哪些看起来不错但不必要的部分
5. 这个功能做完后，首页和主流程会不会更清晰，而不是更复杂
```

## 对 GoalFlow 来说，gstack 这一步主要要产出什么

你希望这一轮不是“多几个想法”，而是拿到下面这些可以继续执行的结果：

- 这个新功能到底是不是 V1 范围内该做的
- 它属于核心闭环还是外围增强
- 它和现有 `目标 / 阶段 / 任务 / 明日代办 / 复盘` 的关系
- 明确的 in scope / out of scope
- 最小可实现版本
- 下一步是继续产品评审，还是进入工程拆解

如果这些还没说清，就不要急着进入开发。

## 这个项目里怎么判断这轮 gstack 用对了

一轮“梳理新功能范围”结束后，好的结果应该满足：

- 没有把 GoalFlow 做偏成日历、打卡或泛待办产品
- 功能范围能明确区分“现在做”和“以后再做”
- 能说清是否影响现有核心推进公式和数据结构
- 能直接进入下一步：`/gstack-plan-ceo-review` 或 `/gstack-plan-eng-review`
- 还没有写代码，但实现边界已经足够清晰

## 你现在就可以这样开始

如果你只是想马上开始，最短路径就是这一句：

```text
/gstack-office-hours
请基于当前 GoalFlow 项目的 PRD.md 和 DEVELOPMENT_SPEC.md，帮我梳理一个新功能的范围，重点明确问题定义、与现有核心模型的关系、V1 最小范围、边界和延后项。先不要写代码。
```
